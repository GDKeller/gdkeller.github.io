---
const { tag, class: className } = Astro.props;
const Element = tag || "div";
---

<Element
  class:list={[
    "pill block w-max flex-nowrap break-keep rounded-full border border-fuchsia-900 bg-fuchsia-950 px-3 py-2 text-body-xs sm:inline-block sm:w-auto sm:px-4 sm:text-body-sm",
    className,
  ]}
>
  <slot />
</Element>
