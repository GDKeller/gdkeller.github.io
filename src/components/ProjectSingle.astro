---
import Link from "./Link.astro";
import ProjectSingleImage from "./ProjectSingleImage.astro";
import Pill from "./Pill.astro";

const { project } = Astro.props;

const { data } = project;
const {
  title,
  projectName,
  client,
  startMonthYear,
  endMonthYear,
  clientWebsite,
  image,
  imageAlt,
  imageAlign,
  imageMaxHeight,
  tech,
} = data;

const { Content } = await project.render();
---

<div class="relative block mb-24 job-card project-card">
  <div
    class="flex flex-col justify-start mb-4 text-2xl font-bayon sm:flex-row sm:text-xl md:text-2xl lg:text-3xl"
  >
    <h3>
      <span class="block font-bold text-fuchsia-500 sm:inline-block">{title}</span>
      <span class="hidden mx-1 font-bokor sm:inline-block"> - </span>
      {
        clientWebsite ? (
          <Link href={clientWebsite} target="_blank" class="no-underline">
            {client}
          </Link>
        ) : (
          <span>{client}</span>
        )
      }
    </h3>
  </div>
  {projectName && <p class="mb-4 text-lg font-bayon sm:text-4xl">{projectName}</p>}
  <p class="mb-8 italic">
    {startMonthYear ? startMonthYear : null} -
    {endMonthYear ? endMonthYear : "Present"}
  </p>

  {
    tech && (
      <div class="mt-0 mb-10 text-xs">
        <p class="sr-only">Skills:&nbsp;</p>
        <ul>
          {tech.map((t: string, i: number) => (
            <>
              <Pill tag="li" class="mb-2" key={i}>
                {t}
              </Pill>
              {i !== tech.length - 1 && " "}
            </>
          ))}
        </ul>
      </div>
    )
  }

  <div
    class:list={[
      "relative",
      { "flex flex-col": imageAlign !== undefined },
      { "md:flex-row-reverse": imageAlign === "right" },
      { "md:flex-row": imageAlign === "left" },
    ]}
  >
    <div
      class:list={["mb-10 shrink-0 md:w-1/3", { "mr-10": imageAlign === "left" }, { "ml-10": imageAlign === "right" }]}
    >
      <ProjectSingleImage imagePath={image} altText={imageAlt || projectName} maxHeight={imageMaxHeight} />
    </div>

    <div class="prose max-w-none job-body">
      <Content />
    </div>
  </div>
</div>

<style>
  .job-body {
    p {
      @apply mb-5;

      &:last-child {
        @apply mb-0;
      }
    }

    li {
      list-style-type: disc;
      list-style-position: outside;
      @apply mb-2 ml-4 pl-2;
    }
  }
</style>
