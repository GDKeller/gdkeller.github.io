---
const { tag, class: className } = Astro.props;
const Element = tag || "div";
---

<Element
  class:list={[
    "pill relative block w-max border-l-2 border-emerald-500/80 px-3 py-1 text-body-xs text-emerald-200 sm:inline-block sm:w-auto sm:px-4",
    className,
  ]}
>
  <slot />
  <span
    class="absolute left-1 top-0 -z-10 h-full w-full bg-gradient-to-r from-emerald-900/40 via-teal-500/15 to-blue-950/20"
  ></span>
</Element>
