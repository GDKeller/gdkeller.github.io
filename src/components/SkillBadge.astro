---
interface Props {
  tag?: string;
  class?: string;
  key?: number;
}

const { tag, class: className } = Astro.props;
const Element = tag || "div";
---

<Element class:list={["text-body-2xs block font-mono tracking-widest sm:inline-block", className]}>
  <slot />
</Element>
