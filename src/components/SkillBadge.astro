---
interface Props {
  tag?: string;
  class?: string;
  key?: number;
}

const { tag, class: className } = Astro.props;
const Element = tag || "div";
---

<Element
  class:list={["block font-mono text-xs font-bold tracking-widest sm:inline-block", className]}
>
  <slot />
</Element>
