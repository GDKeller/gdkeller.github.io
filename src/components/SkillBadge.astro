---
const { tag, class: className } = Astro.props;
const Element = tag || "div";
---

<Element
  class:list={[
    "pill relative block w-max border-l-2 border-emerald-950/80 py-1.5 pl-3 pr-2 text-body-xs text-emerald-200 sm:inline-block sm:w-auto sm:px-4",
    className,
  ]}
>
  <slot />
  <span
    class="absolute left-0.5 top-0 -z-10 h-full w-full bg-gradient-to-br from-emerald-900/30 via-teal-500/20 to-blue-950/40"
  ></span>
</Element>
