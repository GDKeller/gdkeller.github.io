---
import { render } from "astro:content";
import DateRange from "./DateRange.astro";
import Link from "./Link.astro";
import ProjectSingleImage from "./ProjectSingleImage.astro";
import SkillBadge from "./SkillBadge.astro";

const { job } = Astro.props;
const { data } = job;
const {
  company,
  companyLogo,
  companyWebsite,
  position,
  startMonthYear,
  endMonthYear,
  tech,
} = data;
const { Content } = await render(job);
---

<div class="job-frame relative mb-24 xl:mb-32">
  <div
    class="job-card grid grid-cols-1 md:grid-cols-[1fr_10rem] lg:grid-cols-[1fr_12rem] xl:grid-cols-[1fr_16rem]"
  >
    <span class="job-backdrop" aria-hidden="true"></span>

    <div
      class="flex flex-col items-start md:col-start-1 md:row-start-1 md:flex-row md:items-start"
    >
      {
        companyLogo && (
          <div class="logo relative z-20 mr-2 shrink-0">
            <ProjectSingleImage
              imagePath={companyLogo.src}
              altText={companyLogo.alt}
              class="max-h-20 -translate-x-2 -translate-y-6 md:max-h-24 md:-translate-x-4 md:-translate-y-4 lg:max-h-32 xl:max-h-40 xl:-translate-x-8 xl:-translate-y-8 2xl:max-h-48"
            />
          </div>
        )
      }
      <div
        class="flex w-full justify-between pt-4 pr-8 pb-4 pl-8 md:pt-8 md:pl-0 lg:pt-12"
      >
        <div class="flex flex-col justify-start">
          <h3
            class="text-accent font-display relative z-110 mb-4 text-xl leading-snug tracking-wider sm:text-2xl md:text-2xl lg:text-3xl xl:text-4xl"
          >
            {position}
          </h3>

          <div class="relative z-110 flex flex-col items-baseline sm:flex-row">
            <p
              class="text-emphasis block text-xl leading-none font-light tracking-wide max-sm:mb-4 md:text-lg lg:text-xl lg:tracking-wider xl:text-2xl"
            >
              {
                companyWebsite ? (
                  <Link href={companyWebsite} target="_blank" variant="subtle">
                    {company}
                  </Link>
                ) : (
                  company
                )
              }
            </p>
            <span
              class="text-accent mx-2 hidden leading-none sm:inline-block lg:mx-4"
              >&nbsp;᠅&nbsp;</span
            >
            <DateRange
              start={startMonthYear}
              end={endMonthYear}
              class="font-mono text-sm leading-none font-extralight lg:text-base xl:text-lg"
            />
          </div>
        </div>
      </div>
    </div>

    {
      tech && (
        <div class="skills-list border-faint relative z-110 w-full px-8 pt-4 pb-4 md:col-start-2 md:row-span-2 md:row-start-1 md:border-l-2 md:pt-24 md:pl-2 lg:pt-32 lg:pl-4 xl:pl-6">
          <p class="sr-only">Skills:&nbsp;</p>
          <ul class="flex w-full flex-wrap text-left font-mono md:max-w-40 md:flex-col lg:max-w-64">
            {tech.map((t: string) => (
              <li class="text-2xs mr-2 mb-2 flex items-center md:mr-auto md:mb-4 md:items-start lg:text-sm xl:text-base">
                <span
                  aria-hidden="true"
                  class="text-accent triplecolon mr-2 leading-[0.75em] font-light lg:mr-4 xl:mr-6"
                >
                  :::
                </span>
                <SkillBadge
                  tag="span"
                  class="text-emphasis font-extralight uppercase"
                >
                  {t}
                </SkillBadge>
              </li>
            ))}
          </ul>
        </div>
      )
    }

    <div
      class="job-body list-custom-wrapper prose relative z-110 box-content max-w-[80ch] pt-8 pr-8 pb-12 pl-8 font-light tracking-wide md:col-start-1 md:row-start-2 lg:pt-12 lg:pl-12 xl:pr-16 xl:pb-20 xl:pl-24 2xl:pb-24 2xl:pl-32"
    >
      <Content />
    </div>
  </div>
</div>

<style>
  @reference "../styles/global.css";
  .job-backdrop {
    position: absolute;
    inset: 0;
    z-index: 2;
    background: var(--color-background-dark);
    clip-path: polygon(0 0, calc(100% - 10rem) 0, 100% 4rem, 100% 100%, 0 100%);
  }

  .job-frame::after {
    content: "";
    position: absolute;
    inset: -1px;
    z-index: 1;
    pointer-events: none;
    background: var(--color-faint);
    clip-path: polygon(0 0, calc(100% - 5rem) 0, 100% 4rem, 100% 100%, 0 100%);
  }

  @media screen and (min-width: 768px) {
    .job-frame::after {
      inset: -2px;
    }
  }

  .job-frame::before {
    z-index: 0;
    filter: blur(2rem);
    background: radial-gradient(
      circle,
      color-mix(in oklch, var(--color-accent) 100%, transparent) 0%,
      transparent 100%
    );
    content: "";
    top: 0;
    left: 0;
    height: 10rem;
    width: 10rem;
    position: absolute;
    opacity: 0.3;
    transform: translate3d(-25%, -25%, 0);
  }

  @media screen and (min-width: 1024px) {
    .job-frame::before {
      height: 12rem;
      width: 12rem;
    }
  }

  @media screen and (min-width: 1280px) {
    .job-frame::before {
      height: 14rem;
      width: 14rem;
    }
  }
  @media screen and (min-width: 1536px) {
    .job-frame::before {
      height: 16rem;
      width: 16rem;
    }
  }

  .skills-list {
    z-index: 110;
    isolation: isolate;
  }

  @media (prefers-contrast: more) {
    .job-frame::after {
      background: var(--color-emphasis);
    }
    .job-frame::before {
      display: none;
    }
  }
</style>
