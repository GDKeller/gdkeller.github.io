---
import Link from "./Link.astro";

const { job } = Astro.props;
const { data } = job;
const { company, companyWebsite, position, startMonthYear, endMonthYear } = data;

const { Content } = await job.render();
---

<div class="job-card mb-32">
  <div class="mb-2 flex flex-col justify-start text-xl">
    <h3 class="text-2xl font-bold text-fuchsia-500">{position}</h3>
    <!-- <span class="block mx-2"> @ </span> -->
    <Link href={companyWebsite} target="_blank">
      {company}
    </Link>
  </div>

  <p class="mb-8 italic">
    {startMonthYear ? startMonthYear : null} -
    {endMonthYear ? endMonthYear : "Present"}
  </p>

  <div class="job-body prose">
    <Content />
  </div>
</div>

<style>
  .job-body {
    /* line-height: 1.2; */
    p {
      @apply mb-5;
    }
    li {
      list-style-type: disc;
      list-style-position: outside;
      @apply mb-2 ml-4 pl-2;
    }
  }
</style>
